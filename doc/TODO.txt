- add example of how to see all commands that make issues when it
	is run.
- add example of how to pass parameters to make
	like -j 22
- add example of how to seperated source folder from build folder.
	cmake -B flag
- demonstrate other backend generators except make (which is the default).
	cmake --help
	shows you the list of available generators on your platform and in your installation.
- demonstrate the install capabilities of cmake using the 'install' command
- demonstrate if statments and boolean values in cmake
- demonstrate how manipulate lists in cmake using the 'list' command.
- demonstrate how to add options to cmake using the 'OPTION' command.
- demonstrate how to add dependencies to targets.
- demonstrate how to add source to target after they have already been defined.
- do example of what happens with circular dependencies.
